[Unit]
Description=REX FinHub Daily Pipeline Timer (weekdays 5:00 PM ET)

[Timer]
# VPS timezone set to America/New_York — DST handled automatically
# Runs after Bloomberg pull (17:00) has time to complete
OnCalendar=Mon..Fri 17:30:00
Persistent=true

[Install]
WantedBy=timers.target
